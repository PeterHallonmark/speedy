The development focus has been shifted. It used to be a complete binary version 
of Arch Linux initscripts, but the focus is now to create a program which can
start up multiple executebles and scripts in parallell and handle dependencies. 
Pretty much create a tool which can execute the current scripts in parallell in
an easy and bash friendly manner. 

Things that currently are on my TODO list:
------------------------------------------
* Make it work. :-)
